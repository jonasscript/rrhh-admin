# Core framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
a2wsgi>=1.10.0

# OAuth2 / JWT
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# File uploads & multipart
python-multipart==0.0.20

# Configuration
pydantic-settings==2.7.0
python-dotenv==1.0.1

# SSL certificates (needed on macOS / Python 3.13)
certifi>=2024.1.1

# Image handling
Pillow==10.4.0
numpy==1.26.4

# PDF support (PyMuPDF does not require a system Poppler installation)
PyMuPDF>=1.24,<2

# Legacy PDF fallback for environments that already provide Poppler
pdf2image==1.17.0

# YAML template parsing
PyYAML>=6.0

# EasyOCR
easyocr==1.7.2
